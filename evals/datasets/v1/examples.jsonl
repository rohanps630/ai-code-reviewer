{"id": "seed-py-null-deref", "pr_url": null, "pr_title": "Simplify invoice formatting", "pr_diff": "diff --git a/billing.py b/billing.py\n--- a/billing.py\n+++ b/billing.py\n@@ -6,7 +6,5 @@ def format_invoice(user_id: int, users: dict) -> str:\n     user = users.get(user_id)\n-    if user is None:\n-        return \"Invoice for unknown user\"\n-    return f\"Invoice for {user.name}\"\n+    return f\"Invoice for {user.name.upper()}\"\n", "ground_truth": {"findings": [{"category": "bug", "severity": "major", "summary": "Drops the None-check on users.get(...); calling .name on a missing user raises AttributeError at runtime.", "location_hint": "billing.py:8", "source_comment_url": null}], "expected_findings_count": 1, "false_positive_traps": []}, "difficulty": "easy", "added_at": "2026-05-24", "added_by": "rohanpsuresh"}
{"id": "seed-ts-off-by-one", "pr_url": null, "pr_title": "Inclusive upper bound in sumFirstN", "pr_diff": "diff --git a/sum.ts b/sum.ts\n--- a/sum.ts\n+++ b/sum.ts\n@@ -1,7 +1,7 @@\n export function sumFirstN(arr: number[], n: number): number {\n   let total = 0;\n-  for (let i = 0; i < n; i++) {\n+  for (let i = 0; i <= n; i++) {\n     total += arr[i];\n   }\n   return total;\n }\n", "ground_truth": {"findings": [{"category": "bug", "severity": "minor", "summary": "Off-by-one: loop now reads arr[n] which is out of bounds, producing NaN when summed with undefined.", "location_hint": "sum.ts:3", "source_comment_url": null}], "expected_findings_count": 1, "false_positive_traps": []}, "difficulty": "easy", "added_at": "2026-05-24", "added_by": "rohanpsuresh"}
{"id": "seed-react-stale-closure", "pr_url": null, "pr_title": "Tidy Counter interval and add render timestamp", "pr_diff": "diff --git a/Counter.tsx b/Counter.tsx\n--- a/Counter.tsx\n+++ b/Counter.tsx\n@@ -1,11 +1,13 @@\n import { useEffect, useState } from \"react\";\n\n export function Counter({ step }: { step: number }) {\n   const [count, setCount] = useState(0);\n+  const renderedAt = Date.now();\n   useEffect(() => {\n-    const id = setInterval(() => setCount((c) => c + step), 1000);\n+    const id = setInterval(() => setCount(count + step), 1000);\n     return () => clearInterval(id);\n-  }, [step]);\n+  }, []);\n   return <span>{count}</span>;\n }\n", "ground_truth": {"findings": [{"category": "bug", "severity": "major", "summary": "useEffect with empty deps captures the initial count and step; interval callback uses stale closure values instead of the functional updater, so count never advances past step.", "location_hint": "Counter.tsx:7", "source_comment_url": null}], "expected_findings_count": 1, "false_positive_traps": ["The added renderedAt = Date.now() line is dead but harmless; not a bug."]}, "difficulty": "medium", "added_at": "2026-05-24", "added_by": "rohanpsuresh"}
{"id": "seed-sql-injection", "pr_url": null, "pr_title": "Inline SQL query in find_user", "pr_diff": "diff --git a/users.py b/users.py\n--- a/users.py\n+++ b/users.py\n@@ -3,9 +3,9 @@ def find_user(conn, username: str) -> dict | None:\n-    cursor = conn.execute(\n-        \"SELECT * FROM users WHERE username = %s\",\n-        (username,),\n-    )\n+    query = f\"SELECT * FROM users WHERE username = '{username}'\"\n+    cursor = conn.execute(query)\n     row = cursor.fetchone()\n+    if row is None:\n+        return None\n     return dict(row) if row else None\n", "ground_truth": {"findings": [{"category": "security", "severity": "critical", "summary": "Switches from a parameterized query to f-string interpolation of untrusted username, introducing SQL injection.", "location_hint": "users.py:5", "source_comment_url": null}], "expected_findings_count": 1, "false_positive_traps": ["The added `if row is None: return None` is redundant with the existing ternary on the next line but is not a bug; do not flag as a defect."]}, "difficulty": "medium", "added_at": "2026-05-24", "added_by": "rohanpsuresh"}
{"id": "seed-race-condition", "pr_url": null, "pr_title": "Lighten up Cache: drop mutex, add Set/New", "pr_diff": "diff --git a/cache.go b/cache.go\n--- a/cache.go\n+++ b/cache.go\n@@ -1,16 +1,19 @@\n package cache\n\n-import \"sync\"\n-\n type Cache struct {\n-    mu      sync.Mutex\n     entries map[string]string\n }\n\n+func New() *Cache {\n+    return &Cache{entries: map[string]string{}}\n+}\n+\n func (c *Cache) Get(key string) (string, bool) {\n-    c.mu.Lock()\n-    defer c.mu.Unlock()\n     v, ok := c.entries[key]\n     return v, ok\n }\n+\n+func (c *Cache) Set(key, value string) {\n+    c.entries[key] = value\n+}\n", "ground_truth": {"findings": [{"category": "bug", "severity": "critical", "summary": "Removes the sync.Mutex while introducing a concurrent Set; Go maps are not safe for concurrent read/write and will panic with `concurrent map writes` under load.", "location_hint": "cache.go:6", "source_comment_url": null}], "expected_findings_count": 1, "false_positive_traps": ["The new New() constructor is conventional Go and not a defect."]}, "difficulty": "hard", "added_at": "2026-05-24", "added_by": "rohanpsuresh"}
