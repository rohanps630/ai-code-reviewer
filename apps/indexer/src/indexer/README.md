# indexer

Phase 2 module — repo → AST chunks → embeddings → Postgres.

Not implemented in Phase 1. The CLI prints a stub message; the
pipeline modules (`chunking.py`, `embeddings.py`, `pipeline.py`) will
land alongside the Phase 2 RAG work.
