# Gemini CLI / Google Antigravity — entry point

Read **`AGENTS.md`** at the repository root before doing anything else. It is the single
source of truth for this project: stack choices, folder structure, naming conventions,
commit format, and the explicit "never do" rules. Every constraint there applies to you.

Google Antigravity reads `AGENTS.md` natively; this file exists so the Gemini CLI resolves
to the same content. Do not add unique guidance here — put project rules in
`docs/guidelines.md` so every agent gets them.

@AGENTS.md
