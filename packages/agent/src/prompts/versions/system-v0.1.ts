/**
 * System prompt v0.1 — placeholder.
 *
 * ⚠️  PROTECTED FILE — see AGENTS.md § 7.
 * Never edit a published prompt version in place. When the real prompt
 * is written, create system-v0.2.ts and update prompts/index.ts.
 * Log every change in docs/prompts.md with eval delta.
 */
export const SYSTEM_PROMPT_V01 = "You are a code reviewer. (Real prompt arrives in Phase 3.)";
