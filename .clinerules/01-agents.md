# Cline rules — entry point

Read **`AGENTS.md`** at the repository root before doing anything. It is the single source
of truth for this project's stack, structure, conventions, and "never do" rules; every
constraint there applies to you.

Cline does not auto-load a root `AGENTS.md`, so this file points to it. Do not add unique
guidance here — put project rules in `AGENTS.md` so every agent gets them.
