# GitHub Copilot — project instructions

This project's conventions, stack, structure, and hard constraints live in
[`AGENTS.md`](../AGENTS.md) at the repository root. **Read it before suggesting or editing
code**, and follow every "never do" rule there.

This file is a thin pointer so Copilot's VS Code path resolves to the canonical content
(the Copilot coding agent reads `AGENTS.md` natively). Do not add unique guidance here —
project rules belong in `AGENTS.md` so every agent gets them.
